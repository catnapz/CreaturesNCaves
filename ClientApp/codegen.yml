schema: http://localhost:5000/api
generates:
  ./src/api/types.d.ts:
    documents:
      - './src/api/gql/**/*.ts'
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
