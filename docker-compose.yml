version: '3.8'

services:
  creaturesncaves:
    image: cnc:latest
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      NODE_ENV: production
      REACT_APP_SERVER_ADDRESS: http://localhost:80
    ports:
      - "8080:80"